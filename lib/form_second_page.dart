import 'package:flutter/material.dart';

class FormSecondPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Text('Second page'),
      ),
    );
  }
}